[project]
name = "githubsss"
version = "0.1.0"
description = "GitHub Tracker MCP server (read-only)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "fastmcp",
  "httpx",
  "psycopg2-binary>=2.9.11",
  "python-dotenv",
  "sqlalchemy>=2.0.48",
]

[tool.uv]
package = false

